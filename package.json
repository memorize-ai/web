{
	"name": "memorize.ai",
	"scripts": {
		"clean": "rm -rf public/out functions/.next",
		"predeploy": "npm run clean && npm run build -C public",
		"deploy": "firebase deploy",
		"postdeploy": "npm run clean",
		"start": "npm run dev -C public",
		
		"generate-sitemap": "node scripts/src/generate-sitemap",
		"generate-password-reset-email": "node scripts/src/generate-password-reset-email"
	},
	"private": true
}
