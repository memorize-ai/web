case $1 in
	('')
		rm -rf .firebase
		rm -rf elm-stuff
		rm -rf functions/lib
		rm -rf public/elm
		cd functions
		./remove-all
		cd ..
	;;
	('-f'|'--force')
		./clean
		rm -rf node_modules
		rm -rf functions/node_modules
	;;
	(*)
		echo Invalid option $1
	;;
esac