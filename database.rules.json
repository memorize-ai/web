{
	"rules": {
		"users": {
			".read": true,
			".write": true,
			"$uid": {
				".write": "!data.exists()",
				"name": {
					".validate": "newData.isString() && newData.val() != ''"
				},
				"email": {
					".validate": "newData.isString() && newData.val() != ''"
				}
			}
		}
	}
}